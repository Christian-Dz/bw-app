
<article class="card mb-2">
  <div class="card-body">
    <p>{{item.origin}}</p>
    <p>{{item.shortUrl}}</p>
    <a href="/eliminar/{{item._id}}" class="btn btn-danger"> Delete </a>
    <a href="/editar/{{item._id}}" class="btn btn-warning"> Edit </a>
    <button data-short="{{item.shortUrl}}" href="#" class="btn btn-info"> Copy </a>
  </div>
</article>